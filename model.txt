
model {
  for (i in 1:N) {
    z[i] ~ dbern(theta)
    mu_i[i] <- z[i]*mu1 + (1 - z[i])*mu0
    x[i] ~ dnorm(mu_i[i], tau)
  }
  theta ~ dbeta(1, 1)
  mu0 ~ dnorm(0, 1)
  mu1 ~ dnorm(0, 1) T(mu0, )
  sigma ~ dunif(0, 5)
  tau <- 1 / (sigma*sigma)
}
